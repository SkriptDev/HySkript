test "EffTame":
	set {_loc} to event-location
	spawn a sheep at {_loc}:
		set {_e} to event-spawned-entity
		tame {_e}

	wait 1 tick
	assert {_e} is tamed with "The sheep should be tamed now, but found '%npc role of {_e}%'"
