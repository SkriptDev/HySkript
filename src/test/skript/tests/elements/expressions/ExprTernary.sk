test "ExprTernary":
	set {_a} to "test"
	set {_b} to {_a} is set ? {_a} : "something else"
	assert {_b} = "test" with "It should have picked up {_a}"

	set {_b} to {_c} is set ? {_c} : "something else"
	assert {_b} = "something else" with "It should not have picked up {_c}"
